const addUser = async (request, response) => {
  try {
    // add user into orbitDB
  } catch (error) {
    // throw error
  }
};

const loginUser = async (request, response) => {
  try {
    // add user into orbitDB
  } catch (error) {
    // throw error
  }
};

module.exports = {
  addUser,
  loginUser,
};
